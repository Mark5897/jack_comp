if (direction = 4) { do square.moveRight(); }

do Sys.wait(5);  // delays the next movement
return;

class SquareGame {
   field Square square; // the square of this game

while (~(key = 0)) {
    let key = Keyboard.keyPressed();
    do moveSquare();
}

var String name; // test string
let name = "aName";